# ComfyUI-SAM4DBodyCapture Requirements
# ======================================
# Version: 0.4.1 (Mesh Overlay Visualization)

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.20.0
Pillow>=9.0.0
opencv-python>=4.5.0

# Diffusion-VAS dependencies
diffusers>=0.25.0
accelerate>=0.20.0
transformers>=4.30.0
safetensors>=0.3.0

# Model downloads
huggingface_hub>=0.20.0

# Temporal smoothing
scipy>=1.7.0

# Configuration & utilities
omegaconf>=2.0.0
imageio>=2.9.0
tqdm>=4.60.0

# ============================================
# Visualization dependencies (v0.4.1+)
# ============================================
# Required for proper 3D mesh rendering overlay
pyrender>=0.1.45
trimesh>=3.10.0
PyOpenGL>=3.1.0

# For headless rendering (server/docker)
# You may also need: apt-get install libosmesa6-dev freeglut3-dev
# Or use EGL: export PYOPENGL_PLATFORM=egl

# ============================================
# MoGe2 - Camera intrinsics estimation (v0.4.0+)
# ============================================
# OPTIONAL but recommended for accurate camera intrinsics
# Without MoGe2, camera nodes will use fallback FOV=60°
#
# Install with ONE of these methods:
# pip install git+https://github.com/microsoft/MoGe.git
# 
# Or clone and install:
# git clone https://github.com/microsoft/MoGe.git
# cd MoGe && pip install -r requirements.txt

# Uncomment the line below to auto-install MoGe2 from GitHub:
# moge @ git+https://github.com/microsoft/MoGe.git

# Optional: Alembic export (for character point cache)
# alembic  # pip install alembic

# Optional: For GPU acceleration
# xformers  # Uncomment for faster attention (requires CUDA)

# ============================================
# Notes
# ============================================
# 
# Model downloads (~17GB total):
# - diffusion-vas-amodal-segmentation: ~5GB
# - diffusion-vas-content-completion: ~5GB  
# - depth_anything_v2_vitl.pth: ~1.4GB
# - moge-2-vitl-normal: ~5GB (optional, for camera intrinsics)
#
# Models are auto-downloaded to checkpoints/ on first use.
# Or pre-download with: python checkpoint_manager.py --download all
#
# GPU requirements:
# - Minimum: 12GB VRAM (float16)
# - Recommended: 16-24GB VRAM
#
# Install with:
# pip install -r requirements.txt
